name: Build_IPA
on: [push, workflow_dispatch]
jobs:
  build:
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v4
      - uses: subosito/flutter-action@v2
        with:
          channel: 'stable'

      - name: Clean and Build
        run: |
          flutter pub get
          
          # 1. Пересоздаем папку iOS с нуля (чиним битый файл проекта)
          rm -rf ios
          flutter create . --platforms=ios
          
          # 2. МЕНЯЕМ ВЕРСИЮ ТОЛЬКО В НУЖНОМ МЕСТЕ (Безопасно)
          # Правим основной таргет в проекте
          sed -i '' 's/IPHONEOS_DEPLOYMENT_TARGET = 10.0/IPHONEOS_DEPLOYMENT_TARGET = 13.0/g' ios/Runner.xcodeproj/project.pbxproj
          
          # 3. Настраиваем Podfile правильно
          cd ios
          pod init
          # Явно прописываем платформу в начале файла
          echo "platform :ios, '13.0'" > Podfile.tmp
          cat Podfile >> Podfile.tmp
          mv Podfile.tmp Podfile
          
          pod install || true
          cd ..
          
      - name: Build IPA
        run: flutter build ios --release --no-codesign

      - name: Upload
        uses: actions/upload-artifact@v4
        with:
          name: my_app_ipa
          path: build/ios/iphoneos/*.app
